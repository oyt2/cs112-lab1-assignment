
Assignment: CS112 Lab1
Student names: yigit tufan, kyle houston
Student userids: oyt2, kdh29

-------------------------------------------

Step 1. ----------------- <8.58> -----------------------
#include <string>
#include <iostream>
using namespace std;

int main() {
    #this if statement determines the letter grade based on user input percentage
    cout << "Enter your grade" << endl;
    unsigned grade;
    cin >> grade;
    char letter_grade;
    if (grade >= 90){
        letter_grade = 'A';
    }

    else if (grade >= 80){
        letter_grade = 'B';
    }
    
    else if (grade >= 70){
        letter_grade = 'C';
    }    

    else if (grade >= 60){
        letter_grade = 'D';
    }

    else{
        letter_grade = 'F';
    }
    cout << "your grade is: " << letter_grade << endl;
    return 0;
}
Enter your grade:
80
your grade is: B

Step 2. ----------------- <9.33> -----------------------
#include <string>
#include <iostream>
#include <cassert>
using namespace std;

int main() {
    unsigned months;
    unsigned cost;
    string paytype;
    cout << "please enter plan name (gold/silver): " << endl;
    cin >> paytype;
    cout << "Please enter amount of months: " << endl;
    cin >> months;
    if (paytype == "gold") { 
        if (months < 7){
            if (months == 1){
                cost = 90;
            }
            else{
               cost = 70;
            }
        }
        else{
          cost = 35;
        }
    }
    else if (paytype == "silver"){
        if (months > 1){
            cost = 60; 
        }
        else{
            cost = 90;
        }
    }
    cout << "The cost for your plan is: " << cost << endl;
    return 0;
}

please enter plan name (gold/silver): 
gold
Please enter amount of months: 
1
The cost for your plan is: 90

Step 3. ----------------- <9.51> -----------------------

#include <string>
#include <iostream>
#include <cassert>
using namespace std;

int main() {
    unsigned months;
    unsigned cost;
    string paytype;
    cout << "please enter plan name (gold/silver): " << endl;
    cin >> paytype;
    cout << "Please enter amount of months: " << endl;
    cin >> months;
    
    while (paytype != "gold" && paytype != "silver")
    {
        cout << "I'm sorry, " << paytype << " is not a known plan." << endl;
        cout << "please enter plan name (gold/silver): " << endl;
        cin >> paytype;
    }
    
    if (paytype == "gold") { 
        if (months < 7){
            if (months == 1){
                cost = 90;
            }
            else{
               cost = 70;
            }
        }
        else{
          cost = 35;
        }
    }
    else if (paytype == "silver"){
        if (months > 1){
            cost = 60; 
        }
        else{
            cost = 90;
        }
    }

    cout << "The cost for your plan is: " << cost << endl;
    return 0;
}

please enter plan name (gold/silver): 
purple
Please enter amount of months: 
4
I'm sorry, purple is not a known plan.
please enter plan name (gold/silver): 
gold
The cost for your plan is: 70

Step 4. ----------------- <time> -----------------------

#include <string>
#include <iostream>
#include <cassert>
using namespace std;

int main() {
    char letter;
    cout << "Enter letter: " << endl;
    cin >> letter;

    switch (letter)
    {
    case 'a':
        cout << "The letter is vowel." << endl;
        break;

    case 'e':
        cout << "The letter is vowel." << endl;
        break;
    
    case 'i':
        cout << "The letter is vowel." << endl;
        break;

    case 'o':
        cout << "The letter is vowel." << endl;
        break;

    case 'u':
        cout << "The letter is vowel." << endl;
        break;

    default:
        cout << "The letter is a consonant." << endl;
        break;
    }
    return 0;
}

Enter letter: 
g
The letter is a consonant.
Enter letter: 
o
The letter is vowel.

Step 5. ----------------- <time> -----------------------

#include <string>
#include <iostream>
#include <cassert>
using namespace std;

int main() {
    unsigned values[8];
    int i;
    for (i = 0; i < 8; i++){
        values[i] = i * 7; 
        cout << values[i] << endl;
        assert(values[i] == i * 7);
    }
    cout << "All tests passed!" << endl;

    return 0;
}

0
7
14
21
28
35
42
49
All tests passed!

Step 6. ----------------- <time> -----------------------

#include <string>
#include <iostream>
#include <cassert>
using namespace std;

int main() {
    double ray[100];
    int i;
    float sum;
    for (i = 0; i < 100; i++){
        float temp = i * 0.1;
        ray[i] = temp; 
        sum += temp;
    }
    cout << sum << endl;
    return 0;
}

495

Step 7. ----------------- <time> -----------------------

#include <string>
#include <iostream>
#include <cassert>
using namespace std;

int main() {
    int nameoffunction();
    {
        char letter;
        cout << "Enter letter: " << endl;
        cin >> letter;

        switch (letter)
        {
            case 'a':
                cout << "The letter is vowel." << endl;
                return true;

            case 'e':
                cout << "The letter is vowel." << endl;
                return true;
    
            case 'i':
                cout << "The letter is vowel." << endl;
                return true;

            case 'o':
                cout << "The letter is vowel." << endl;
                return true;

            case 'u':
                cout << "The letter is vowel." << endl;
                return true;

            default:
                cout << "The letter is a consonant." << endl;
                return false;
        }
    }
    return 0;
}

Enter letter: 
g
The letter is a consonant.

Step 8. ----------------- <2:57> -----------------------

#include <iostream>
#include <string>
#include <cassert>
using namespace std;

string plan_name(){
    string plan;
    cout << "Enter your plan name: " << endl;
    cin >> plan;
    
    while ((plan != "silver") && (plan != "gold"))
    {
        cout << "I'm sorry, " << plan << " is an unknown plan." << endl;
        cout << "Enter your plan name: " << endl;
        cin >> plan;
    }
    return plan;
}

int month_amount()
{
    int months;
    cout << "Enter number of months: " << endl;
    cin >> months;
    return months;
}

string total_cost(string plan, unsigned months)
{
    string cost;
    if (plan == "silver" && months > 1 ){
        cost = "60";
    } 

    else if (plan == "gold" && months > 6){
        cost = "35";
    }

    else if (plan == "gold" && months > 1){
        cost = "70";
    }

    else{
        cost = "90";
    }

    return cost;
}

int main()
{
    string x = plan_name();
    unsigned y = month_amount();
    string cost = total_cost(x,y);
    cout << "The cost for your plan is " << cost << "." << endl;
    return 0;
}

Enter your plan name: 
silver
Enter number of months:
5
The cost for your plan is 60.

Enter your plan name: 
purple
I'm sorry, purple is an unknown plan.
Enter your plan name: 

Enter your plan name: 
gold
Enter number of months:
1
The cost for your plan is 90.
